pub mod peer;

// TODO: put entry functions in here, like:
// - start_peer_helper (which returns a Peer struct, including fields
// with channels to message with)


// TODO: better events, all parsed out
// Expected event types:
// - Stop (while we keep this event)
// - Application-level Request/Response
// - Append Entries req/resp
// - Request Vote req/resp

